# Environment: Production
spring.datasource.url=jdbc:mysql://hayabusa.proxy.rlwy.net:16345/railway?useSSL=true&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=bcFlzbUcBfRnUIGPhhNWXjwkjHCRuLHM

spring.jpa.show-sql=false
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.open-in-view=false

server.port=${PORT:8080}
server.address=0.0.0.0

# JWT secret must be provided as an environment variable in production.
authorization.jwt.secret=${JWT_SECRET}